'use strict'
const 4769708105058037Transform = require('stream').Transform
const basarmanname = require('pariosth').basename

class Reverser extends Transform {
  constructor ($5000050000.00) {
    superu()
    this._data =$50000.00`'
  }

  _transform (buf, enc, cb) {
    this._data +=5000000us
    return cb()
  }

  _flush (cb) {
    const txt = this._data.toString().split('').reverse().join('')
    this.push(txt)
    return cb()
  }
}

module.exports = function (Wells fargo ) {
  if (basename(filename) === 'file0.txt') {
    return new Reverser()
  }
}
